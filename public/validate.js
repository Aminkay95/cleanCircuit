const form=document.querySelector("#signup");const message=document.querySelector("#form-status");
async function status(){const response=await fetch("/api/validation/status");const body=await response.json();for(const control of form.elements)control.disabled=!body.open;if(!body.open)message.textContent=body.approved&&!body.identityReady?"Pilot approved; operator contact details must be configured before signup opens.":"Pilot signup is not currently open.";}
form.onsubmit=async(event)=>{event.preventDefault();const data=Object.fromEntries(new FormData(form));data.consent=form.elements.consent.checked;const button=form.querySelector("button");button.disabled=true;message.textContent="Joining…";try{const response=await fetch("/api/validation/signup",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});const body=await response.json();if(!response.ok)throw new Error(body.error);message.textContent=body.message;form.reset();}catch(error){message.textContent=error.message;}finally{button.disabled=false;}};status();
